#include <stdlib.h>
#include <string.h>
#include <unistd.h>

int main(int argc, char **argv) {
    char buf[40];
    int size;

    size = atoi(argv[1]);
    
    if (size < 10) {
        memcpy(buf, argv[2], size * 4);
        
        if (size == 0x574f4c46) {// 1464814662
            execl("/bin/sh", "sh", NULL);
        }
        return 0;
    }
    
    return 1;
}
